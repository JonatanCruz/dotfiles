# =============================================================================
# API Keys & Secrets — Template
# =============================================================================
# Copy this file to secrets.zsh and fill in your values:
#   cp secrets.zsh.example secrets.zsh
#
# secrets.zsh is gitignored and NEVER committed to the repository.
# =============================================================================

# Tavily (web search MCP)
export TAVILY_API_KEY=""

# 21st.dev Magic (UI components MCP)
export MAGIC_API_KEY=""

# Add other API keys below
