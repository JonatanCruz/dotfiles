# ==============================================================================
# .zshenv - Variables de entorno fundamentales
# ==============================================================================
# Este archivo se carga SIEMPRE, en todas las shells (interactivas y no interactivas)
# Úsalo para definir variables de entorno esenciales como ZDOTDIR

# Directorio de configuración de zsh
export ZDOTDIR="${HOME}/.config/zsh"
